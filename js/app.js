const swiper = new Swiper('.swiper', {
    speed: 400,
    spaceBetween: 100,
    loop: true,
    autoplay: {
        delay: 5000,
    },
    // effect: 'fade',
    // fadeEffect: {
    //   crossFade: true
    // }
});

// const swiper = document.querySelector('.swiper').swiper;